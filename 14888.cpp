#include <bits/stdc++.h>
using namespace std;

int dir[4];

void dfs(int sum){
    for(int i=0;i<4;i++){
        if(dir[])
    }
}

int main(){
    int num; cin >> num;
    vector<int> v(num);
    for(int i=0;i<num;i++) cin >> v[i];
    for(int i=0;i<4;i++) cin >> dir[i];
    dfs(0);

}